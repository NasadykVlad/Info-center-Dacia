 <h1>Edit contact: {{contact.name}}</h1>
 
 <div class="row form_for_help">
    <p>If you need advice on choosing details, leave your details in the form below</p>
    <form action="/shop/edit" method="POST" class="col s12">
      <div class="row">
        <div class="input-field col s4">
          <input name="name" type="text" class="validate" required value="{{contact.name}}">
          <label>Name</label>
          <span class="helper-text" data-error="Enter YOUR name"></span>
        </div>
        <div class="input-field col s4">
          <input name="photo" type="tel" class="validate" required value="{{contact.photo}}">
          <label>Photo</label>
          <span class="helper-text" data-error="Enter YOUR mobile number"></span>
        </div>
        <div class="input-field col s4">
          <input name="email" type="number" class="validate" required value="{{contact.email}}">
          <label>Price</label>
          <span class="helper-text" data-error="Enter YOUR email"></span>
        </div>
      </div>
      <div class="edit-delete-buttons">
      <input type="hidden" name="id" value="{{contact.id}}">
      <input type="hidden" name="_csrf" value="{{csrf}}">
      <button type="submit" class="btn btn-primary">Edit</button>
    </form>
    <form style="display: inline-block; margin-left: 10px" action="/shop/remove" method="POST">
    <input type="hidden" name="id" value={{contact.id}}>
    <input type="hidden" name="_csrf" value="{{csrf}}">
    <button class="btn red">Delete contact</button>
    </form>
    </div>
  </div>