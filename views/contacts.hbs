<h1>Contacts page</h1>

{{#if contacts.length}}
{{#each contacts}}
<div class="row">
    <div class="col s6 offset-s3">
      <div class="card">
        <div class="card-image">
          <img src="{{photo}}">
        </div>
        <div class="card-content">
          <p>{{email}}</p>
          <span class="card-title name">{{name}}</span>
        </div>
        <div class="card-action">
          <a href="/contacts/{{id}}">Open contact</a>
        </div>
      </div>
    </div>
  </div>
{{/each}}
{{else}}
<p>No course...</p>
{{/if}}